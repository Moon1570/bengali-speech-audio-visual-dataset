# Bengali Speech Audio-Visual Dataset - Pipeline Configuration
# =============================================================
# This file contains configuration settings for the complete pipeline

# Paths
CURRENT_REPO="/Users/darklord/Research/Audio Visual/Code/bengali-speech-audio-visual-dataset"
SYNCNET_REPO="/Users/darklord/Research/Audio Visual/Code/syncnet_python"

# Default Parameters
DEFAULT_MAX_WORKERS=8
DEFAULT_MIN_CHUNK_DURATION=2
DEFAULT_PRESET="high"

# Step Controls (set to "true" to skip a step)
SKIP_STEP1=false  # Skip chunk creation
SKIP_STEP2=false  # Skip SyncNet filtering  
SKIP_STEP3=false  # Skip directory organization
SKIP_STEP4=false  # Skip transcription

# Pipeline Parameters
FILTER_FACES=true
REFINE_CHUNKS=true
TRANSCRIPTION_MODEL="both"  # Options: google, whisper, both
BATCH_MODE=true

# SyncNet Parameters
SYNCNET_PRESET="high"  # Options: low, medium, high

# Logging
ENABLE_DETAILED_LOGGING=true
LOG_DIRECTORY="logs"
